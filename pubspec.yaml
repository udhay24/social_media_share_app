name: social_media_share_app
description: A new Flutter application.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^0.1.3
  social_share: ^2.0.5
  image_picker: ^0.6.7+7
  fish_redux: ^0.3.4
  flutter_hooks: ^0.14.1
  image_gallery: ^1.2.0
  flutter_icons: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

